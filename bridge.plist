<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  macOS LaunchAgent for Copilot Teams Bridge

  Install:
    cp bridge.plist ~/Library/LaunchAgents/com.copilot-teams-bridge.plist
    launchctl load ~/Library/LaunchAgents/com.copilot-teams-bridge.plist

  Uninstall:
    launchctl unload ~/Library/LaunchAgents/com.copilot-teams-bridge.plist
    rm ~/Library/LaunchAgents/com.copilot-teams-bridge.plist

  Logs:
    tail -f /tmp/copilot-teams-bridge.out.log
    tail -f /tmp/copilot-teams-bridge.err.log
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.copilot-teams-bridge</string>

    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/python3</string>
        <string>-m</string>
        <string>bridge.daemon</string>
    </array>

    <key>WorkingDirectory</key>
    <!-- Update this to your copilot-teams-bridge directory -->
    <string>/Users/you/copilot-teams-bridge</string>

    <key>EnvironmentVariables</key>
    <dict>
        <!-- Ensure Copilot CLI and gh are in PATH -->
        <key>PATH</key>
        <string>/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin</string>
    </dict>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/tmp/copilot-teams-bridge.out.log</string>

    <key>StandardErrorPath</key>
    <string>/tmp/copilot-teams-bridge.err.log</string>

    <key>ThrottleInterval</key>
    <integer>10</integer>
</dict>
</plist>
